{
  "functions": [{ "source": "functions", "codebase": "notifications", "predeploy": ["npm --prefix functions run build"], "ignore": ["node_modules", "test", "*.log"] }],
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
